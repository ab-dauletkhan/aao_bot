[phases.setup]
nixPkgs = ["python3", "gcc", "build-essential"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  "/opt/venv/bin/pip install --upgrade pip",
  "/opt/venv/bin/pip install -r requirements.txt"
]

[phases.start]
cmd = "/opt/venv/bin/python -m bot.main"
